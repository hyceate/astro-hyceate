---
import Layout from "@layouts/Layout.astro";
import Hero from "@components/hero.svelte";
import { isReady } from "@lib/stores/stores";
---

<!-- INDEX HOMEPAGE -->
<Layout
	title="Hyceate's Portfolio"
	description="Hyceate's Portfolio for Live2d related works and digital illustrations."
>
	{isReady ? <Hero client:only="svelte" /> : null}
</Layout>
<style></style>
